import 'package:flutter/material.dart';

/// Identité visuelle Sandokti (Maroc Digital)
class SandoktiColors {
  /// Vert Maroc (inspiré du drapeau)
  static const emerald = Color(0xFF006747);

  /// Or mat pour les gains / highlights premium
  static const gold = Color(0xFFD4AF37);

  /// Encre profonde (texte/contraste)
  static const ink = Color(0xFF0F172A);

  /// Fond clair premium
  static const offWhite = Color(0xFFF8F6F1);

  /// Danger
  static const danger = Color(0xFFDC2626);

  /// Neutres
  static const slate = Color(0xFF94A3B8);
}